{% set article_show =   path('article_show', { 'slug': article.slug })  %}

<article class="mb-5">
    <div class="row">
        <div class="col-md-{{ leftCol ??5 }} ">

            {% if article.featuredImage %}
                <a href="{{ article_show }}">
                    <img src="/uploads/{{ article.featuredImage.filename}}" alt="{{ article.featuredImage.altText }}" loading="lazy" width="350" height="205">
                </a>

            {% endif %}
        </div>

        <div class="col-md-{{ rightCol ??7 }}">
            <h2>
                <a class="text-decoration-none" href="{{  article_show }}">{{ article.title }}</a>
            </h2>
            <p>
                {{ article.createdAt|date('d M Y')}}
            </p>

            {{ article.featuredText|raw ?: article.content|raw|striptags|slice(0, 130) ~ '...'}}
            {# le filtre raw permet de supprimer les balises html à l'affichage #}
        </div>
    </div>
</article>